# 问题记录

## MuJoCo viewer 启动后不显示模型

- **现象**：`python -m mujoco.viewer test2.xml` 打开 viewer 但画面空白
- **原因**：MuJoCo 3.13.0 的 viewer CLI 必须用 `--mjcf=` 参数指定文件，直接传文件名会被忽略
- **解决**：
  ```bash
  python -m mujoco.viewer --mjcf=test2.xml
  ```
- **验证**：`python -m mujoco.viewer --helpfull` 查看支持的参数

## test2.py 运行后模型运动过快/物理时间不同步

- **现象**：`python test2.py` 打开 viewer 后模型运动速度异常快
- **原因**：原代码用 `time.time()` + 累积期望时间 (`start_time + data.time`) 做同步，但 Windows 上 `time.time()` 精度低（~1-15ms），且累积误差随步数增长
- **解决**：改用 `time.perf_counter()` 每步独立计时，精度更高（纳秒级），无累积误差
  ```python
  step_start = time.perf_counter()
  mujoco.mj_step(model, data)
  viewer.sync()
  elapsed = time.perf_counter() - step_start
  if elapsed < dt:
      time.sleep(dt - elapsed)
  ```
- **说明**：`time.perf_counter() ` 是 Windows 上最高精度的计时器，适合微秒级的时间对齐